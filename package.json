{
  "name": "paulyform",
  "private": true,
  "description": "VoraForm — Uncontrolled-first, zero re-render form library for React",
  "scripts": {
    "typecheck": "pnpm -r typecheck && tsc -p registry/tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  },
  "dependencies": {
    "@types/react": "^18.3.28",
    "@types/react-dom": "^18.3.7",
    "dompurify": "^3.3.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}
